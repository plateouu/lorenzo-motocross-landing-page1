export default function Loading() {
    return (
        <div className="fixed inset-0 bg-off-white flex items-center justify-center z-[50]">
            <span className="font-serif text-xl tracking-widest uppercase animate-pulse">Loading</span>
        </div>
    )
}
